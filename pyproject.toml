[project]
name = "tpuchat"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mlx>=0.22.0",
    "tiktoken",
    "pyarrow",
    "huggingface_hub",
    "requests",
    "matplotlib",
    "jax>=0.9.0.1",
]
